.next/
node_modules/
.env
.env.*
!.env.example
.DS_Store
*.log
dist/
next-env.d.ts
tsconfig.tsbuildinfo

# ML — runtime data (lives on Railway volumes, not in git)
ml/**/__pycache__/
ml/**/*.pyc
ml/nba_spread/data/
ml/world_cup/data/

# Model binary is committed — it needs to ship with the Railway image.
# Revisit only if it grows beyond ~50MB.
# Visual workflow artifacts
.auth/
artifacts/
