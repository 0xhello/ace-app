.next/
node_modules/
.env.local
.env
*.log
dist/
next-env.d.ts
tsconfig.tsbuildinfo

# ML — runtime data (lives on Railway volumes, not in git)
ml/**/__pycache__/
ml/**/*.pyc
ml/nba_spread/data/

# Model binary committed (<1MB) — Railway needs it for inference
# ml/nba_spread/artifacts/*.joblib
