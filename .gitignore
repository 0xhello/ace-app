.next/
node_modules/
.env.local
.env
*.log
dist/
next-env.d.ts
tsconfig.tsbuildinfo

# ML — runtime data files (live on Railway volume, not in git)
ml/**/__pycache__/
ml/**/*.pyc
ml/nba_spread/data/
ml/nba_spread/model_performance.csv

# Model binary is committed — it's <1MB and Railway needs it for inference
# ml/nba_spread/artifacts/*.joblib
